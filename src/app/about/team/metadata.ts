import { createPageMetadata } from "@/lib/seo";

export const metadata = createPageMetadata(
  "Our Team",
  "Meet the RSK Associates team — corporate advisors, strategists, and professionals dedicated to helping businesses move forward with confidence.",
  "/about/team"
);