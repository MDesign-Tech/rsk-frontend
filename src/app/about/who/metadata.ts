import { createPageMetadata } from "@/lib/seo";

export const metadata = createPageMetadata(
  "Who We Are",
  "Learn about RSK Associates — a corporate advisory collective helping businesses navigate growth, finance, and strategy with confidence.",
  "/about/who"
);