import { createPageMetadata } from "@/lib/seo";

export const metadata = createPageMetadata(
  "Contact Us",
  "Get in touch with RSK Associates for professional services, consulting, and business advisory.",
  "/contact"
);